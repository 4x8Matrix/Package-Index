-- local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- local Console = require(ReplicatedStorage.Packages.Console)

-- local reporter = Console.newOrphaned("Orphan-Log")

-- task.spawn(function()
-- 	reporter:Critical(1)
-- end)

-- Console.setGlobalLogLevel(Console.LogLevel.Critical + 10)

-- task.spawn(function()
-- 	reporter:Critical(2)
-- end)
